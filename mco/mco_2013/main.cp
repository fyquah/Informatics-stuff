#include <iostream>
#include <algorithm>
using namespace std;

int T , N , M , K;
int map[302][302];
int ans[302][302];

int main()
{
	cin >> T;
	for(int counter = 0; counter < T ; counter++)
	{
		cin >> N >> M >> K;
		memset(map , 0 , sizeof(map));
		memset(ans , 0 , sizeof(map));
		for(int i = 0 ; i < N ; i++){
			
		}

	}
	return 0;
}